CAPI=2:
name: "{name}:{name}_pkg:0.1"
description: "Header package for {name}"

filesets:
  files_rtl:
    files:
      - rtl/{name}_pkg.sv
    file_type: systemVerilogSource

targets:
  default:
    filesets:
      - files_rtl
